class ExprElem{
	readonly expr: Expr;
	readonly div: DDiv;
	readonly del: DButton;
	readonly label: DSpan;
	readonly id: DSpan;
	readonly sep: DSpan;

	constructor(){
	}
	setid(i: number){
		FIXME
		s.obj.setlabel("[" + i + "] " + s.rule.label);
		FIXME: s.obj.label.textContent = ...;
	}
}
